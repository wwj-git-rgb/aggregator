interface:
  display_name: "Aggregator"
  short_description: "Repo-specific patterns and workflows for aggregator"
  default_prompt: "Use the aggregator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true